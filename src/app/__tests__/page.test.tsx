import { describe, it } from "node:test";

describe("Homepage", () => {
  it('Should have title text', () => {

  })

  it('Should show modal when request button is clicked', () => {

  })

  it('Should call API when send button is clicked', () => {

  })
})